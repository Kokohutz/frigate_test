#!/command/with-contenv bash
# shellcheck shell=bash
# Start the detection-bridge Rust daemon

set -o errexit -o nounset -o pipefail

exec 2>&1
echo "[INFO] Starting detection-bridge..."
exec /usr/local/bin/detection-bridge
