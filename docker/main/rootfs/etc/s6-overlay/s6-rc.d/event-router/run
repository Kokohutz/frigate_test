#!/command/with-contenv bash
# shellcheck shell=bash
# Start the event-router Rust daemon

set -o errexit -o nounset -o pipefail

exec 2>&1
echo "[INFO] Starting event-router..."
exec /usr/local/bin/event-router
