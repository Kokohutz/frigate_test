#!/command/with-contenv bash
# shellcheck shell=bash
# Start the tiered-storage Rust daemon

set -o errexit -o nounset -o pipefail

exec 2>&1
echo "[INFO] Starting tiered-storage..."
exec /usr/local/bin/tiered-storage
